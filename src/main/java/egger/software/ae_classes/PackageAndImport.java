package egger.software.ae_classes; //Packages are namespaces. Always structured like the directory!

import static java.lang.Math.abs;

public class PackageAndImport {
    static int absTest = abs(-10);
}
